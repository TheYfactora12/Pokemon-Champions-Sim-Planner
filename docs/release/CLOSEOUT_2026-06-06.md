# Closeout Status - 2026-06-06

## Current Branch

- Branch: `merge-candidate/alfredo-main-2026-06-06`
- Latest commit before this closeout note: `8f2bef9 Merge Alfredo main into sync candidate`
- Branch status: clean and pushed to `origin`
- Pull request status: not open yet

Create the PR here:

```text
https://github.com/TheYfactora12/Pokemon-Champions-Sim-Planner/pull/new/merge-candidate/alfredo-main-2026-06-06
```

## Fixed Today

- Restored the top-level `Review` tab and kept the panel heading as `Review Overview`.
- Added the top-level `Overview` tab with shipped work, validation proof, current gaps, next milestones, open decisions, and source-document links.
- Added stable Pokemon identity fields to exported battle state so Pokemon, items, HP, active slots, bench slots, and consumed items can stay tied to the registered Pokemon across switches and faints.
- Added `tools/validate-turn-logs.mjs` to audit exported `champions-turn-log-*.json` files for identity drift, item drift, HP key drift, active/bench key drift, speed-order coverage, and observed priority/speed ordering.
- Aligned local move priority with generated Showdown metadata for known drift cases such as `Feint`, `Ice Shard`, and Protect-family shield moves.
- Added Showdown priority drift coverage so new upstream priority mismatches are easier to detect.
- Wrote the Showdown DB source-of-truth plan: Showdown-mirrored rows first, Champions override rows second, generated app assets last.
- Merged Alfredo `main` into the merge candidate branch and resolved the service-worker cache conflict.

## Verified

- The live GitHub Pages preview was reachable and contained the new Overview tab.
- Main branch GitHub Actions and Pages checks were green after the Overview work.
- The merge candidate branch was locally clean after push.
- The merge candidate ran a CI-shaped JS test sweep with zero failures before this closeout note.
- User-provided legacy logs passed non-strict validation with zero hard identity, item, HP, active/bench, or priority-order errors.

## Still Open

- Open the PR from `merge-candidate/alfredo-main-2026-06-06` into `main`.
- Let GitHub PR CI and Pages checks run on the PR.
- Review the Alfredo merge candidate before merging, especially the merged service-worker cache version and new M8 prior-snapshot migrations.
- Hard refresh the deployed preview and export fresh logs that include stable identity fields.
- Run strict turn-log validation on new exports:

```bash
cd poke-sim
node tools/validate-turn-logs.mjs --require-stable path/to/champions-turn-log.json
```

## Showdown And Supabase State

The current app is not fully DB-backed for Showdown mechanics yet.

Already connected:

- existing Supabase app tables such as teams and analyses
- generated/static Showdown data used by the browser bundle
- priority drift tests against generated Showdown metadata

Still planned:

- live application of the staged `showdown_entities`, `showdown_entity_diffs`, and `champions_overrides` migration
- live `approved_showdown_entities` and `approved_champions_data` views
- production generation from approved DB views into deterministic JS assets
- scheduled sync that detects upstream Showdown changes and reports or opens reviewed PRs

## Closeout Definition

This work is closed for today when:

1. The merge candidate PR is opened.
2. PR CI passes.
3. The PR is reviewed and merged.
4. The deployed Pages preview updates from `main`.
5. One fresh hard-refreshed log passes strict validation.
